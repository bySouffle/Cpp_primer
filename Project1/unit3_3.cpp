#include<iostream>
#include<vector>
using namespace std;

//int main()
//{
//	vector<int> ivec;
//	vector<int> ivec2(ivec);
//	vector<int>	ivec3 = ivec;
//	// vector<string> svec(ivec2); 向量类型不同不能赋值
//
//	vector<string> v1 = { "a","b","c" };
//	//	vector<string> v2 = ("a", "b", "c");	{}初始化
//	
//	// (n,m) n个m
//	vector<int> ivec_db(10, -1);
//	vector<string>	svec_db(10, "A");
//
//	//	(n) n个元素int 初始化为0 string 初始化为空
//	vector<int> ivec_sl(10);
//	vector<string>	svec_sl(10);
//
//	// vector<int> ivec_sl = 10;	只能直接初始化 而非拷贝
//
//
//}